import { React } from 'react';
import Button from '@mui/material/Button';

const BackButton = () => {
    return (
        <Button variant="outlined" color="error" style={{ width: "50px" }}>
            Back
        </Button >
    )
}

export default BackButton;